The programme is a simple terminal applciation for a todo list
It uses the pickle module to create store and update the data entered
It has the basic functions like-
create new to create a new data file to store the data
delete update search and read as per the users choice
the menu function controls the flow of the programme based on user inputs

modules used-pickle(to store write and read data)
            -tabulate(to print data in the terminal which is easily readable)
            -time(ti delay the printing of some statements)
